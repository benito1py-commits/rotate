export interface HandLandmark {
  x: number;
  y: number;
  z: number;
}

export interface GameMeta {
  id: string;
  title: string;
  description: string;
  emoji: string;
  color: string;
  path: string;
}
